/* =========================================================
   ota.js — 폐기됨(26.9.21)
   ─────────────────────────────────────────────────────────
   ⛔ OTA(무선 웹 자산 교체)는 쓰지 않는다. 이유 두 가지다.

   1) 전달 사고 — OTA 로 웹만 갈아끼우면 네이티브 코드와 버전이 어긋나 원인 못 찾는
      결함이 반복됐다. 그래서 「선 연결 빌드만」 으로 방침을 바꿨다.
   2) 스토어 위험 — 앱 콘텐츠를 원격에서 통째로 교체하는 구조는 심사에서
      「설치 후 앱이 달라진다」로 읽힐 수 있다. 광고 SDK 사고로 앱이 내려간
      전례(피키캐스트)를 겪은 만큼, 사소한 의심거리도 남기지 않는다.

   Capgo 플러그인은 설치돼 있지 않아 원래도 동작하지 않았지만,
   누군가 `npm i @capgo/capacitor-updater` 한 줄로 되살릴 수 있어 내용을 비웠다.
   되살리려면 이 주석을 먼저 읽고 위 두 가지를 어떻게 막을지 정한 뒤에 하라.
   ========================================================= */
