/* 이미 적용됐지만 추적 테이블에 기록이 없던 22개를 채운다.
   Management API 로 직접 SQL 을 돌리면 supabase_migrations.schema_migrations 에
   안 들어간다 — 그 상태로 db push 를 하면 385개를 처음부터 다시 실행하려 든다.
   각 파일이 만드는 표 5개·함수 14개가 DB 에 실재함을 확인하고 기록만 채웠다.
   (이 파일 자체는 기록용이며 아무것도 바꾸지 않는다 — 실제 삽입은 2026-09-01 에 끝냈다.) */
select 1;
